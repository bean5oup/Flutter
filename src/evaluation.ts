export async function evaluate(): Promise<{ 'score': number }> {
    try {
        // here scoring
        
        return {
            'score': 10
        };
    } catch (error) {
        throw error;
    }
}